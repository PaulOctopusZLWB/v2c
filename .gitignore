.DS_Store
.venv/
.smoke-data/
.smoke-vault/
.smoke-nas/
.smoke-launchd/
.smoke-config.toml
data/
build/
__pycache__/
*.py[cod]

# Local sample recordings (large WAV fixtures) and scratch/probe dirs — never commit
sample_data/
.tmp/

# Frontend (web control panel) build + deps — never commit
web/node_modules/
web/dist/
web/*.tsbuildinfo

# Design/docs are kept local only — GitHub holds code, not design (*.md untracked)
*.md
web/shot.mjs
web/shot-hl.mjs
web/shot*.mjs
